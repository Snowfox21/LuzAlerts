# Скопируй в .env и заполни значения
# cp .env.example .env

# База данных
POSTGRES_PASSWORD=changeme

DATABASE_URL=postgresql+asyncpg://luz:changeme@db:5432/luzalerts

# Nominatim (обязательно укажи реальный email — требование ToS)
NOMINATIM_EMAIL=your@email.com

# Admin API (сгенерировать: openssl rand -hex 32)
ADMIN_API_KEY=
